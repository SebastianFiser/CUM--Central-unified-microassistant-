import 'package:flutter/material.dart';

class FancyB extends StatelessWidget {
  const FancyB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('FancyB (placeholder)'));
  }
}
